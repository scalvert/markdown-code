# Demo

```typescript
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
```

```json
{
  "name": "my-project",
  "version": "1.0.0"
}
```

```javascript
function calculateSum(a, b) {
  return a + b;
}
```

```bash
npm install
npm start
``` 